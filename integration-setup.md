# 키오스크 로그 연동 설정

이 프로토타입에서 말하는 Google 연동은 문서 작성용 Google Docs가 아니라 표 형태 로그 저장용 **Google Sheets**입니다.

Supabase는 백엔드/DB 역할입니다. 키오스크가 토출 로그를 Supabase 테이블에 저장하면 운영자 페이지, 관리자 웹, 재고 관리 API에서 같은 데이터를 안정적으로 조회할 수 있습니다.

Google Sheets는 PoC 단계에서 클라이언트가 쉽게 열어보는 로그 장부 역할로 볼 수 있습니다. 운영 정식 버전에서는 Supabase를 기준 저장소로 두고, Sheets는 보조 리포트나 임시 확인용으로 쓰는 구성이 더 자연스럽습니다.

## Supabase

1. Supabase에서 프로젝트를 생성합니다.
2. SQL Editor에서 아래 테이블을 만듭니다.

```sql
create table if not exists vending_logs (
  id bigint generated by default as identity primary key,
  device_id text not null,
  selected_need text,
  product_name text,
  product_id text,
  slot text,
  cartridge integer,
  quantity integer,
  dispense_status text,
  created_at timestamptz default now()
);
```

3. Project Settings > API에서 `Project URL`과 `anon public key`를 확인합니다.
4. 프로토타입의 운영자 화면에 아래 값을 입력합니다.
   - Supabase URL
   - Supabase anon key
   - Supabase table: `vending_logs`

## Google Sheets

1. Google Sheets를 하나 만듭니다.
2. 확장 프로그램 > Apps Script를 엽니다.
3. `google-sheets-apps-script.js` 내용을 붙여 넣습니다.
4. 배포 > 새 배포 > 웹 앱을 선택합니다.
5. 실행 권한은 본인, 액세스 권한은 테스트 목적에 맞게 설정합니다.
6. 발급된 Web App URL을 프로토타입 운영자 화면의 `Google Apps Script Web App URL`에 입력합니다.

## 동작 흐름

키오스크 화면에서 상태를 선택하면 추천 상품이 바뀝니다.
토출 버튼을 누르면 현재 추천 상품 기준으로 로그 payload를 만들고 Supabase와 Google Sheets에 동시에 전송합니다.

연동값이 비어 있으면 실제 전송은 건너뛰고 `설정 필요`로 표시됩니다.
